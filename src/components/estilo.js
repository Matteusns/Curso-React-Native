import { StyleSheet } from "react-native"

export default StyleSheet.create({
    App: {
        backgroundColor: '#ddd',
        flexGrow: 1,
        justifyContent: 'center',
        alignItems: 'center',
        padding: 10
    },
    txtG: {
        fontSize: 32
    }
})